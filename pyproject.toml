[project]
name = "scriptcert-pearl"
version = "0.1.0"
description = "A tiny formally verified JavaScript-compiler pearl"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[tool.unittest]
# Uses Python stdlib unittest via `python3 -m unittest discover -s tests -v`.

